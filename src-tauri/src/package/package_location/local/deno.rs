use std::path::Path;

use crate::package::{package_location::PackageLocation, Package};

pub async fn resolve_deno_package_info(path: &Path, location: &PackageLocation) -> Vec<Package> {
    vec![]
}
